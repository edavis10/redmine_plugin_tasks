module <%= options[:plugin_name].underscore.camelize %>
  module Hooks
    class <%= options[:hook_name].underscore.camelize -%>Hook < Redmine::Hook::ViewListener
      def <%= options[:hook_name].underscore -%>(context={})
        return ''
      end
    end
  end
end
