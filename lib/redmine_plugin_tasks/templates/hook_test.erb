require File.dirname(__FILE__) + '/../../../test_helper'

class <%= options[:plugin_name].underscore.camelize -%>::Hooks::<%= options[:hook_name].underscore.camelize -%>Test < ActionController::IntegrationTest
  include Redmine::Hook::Helper

  context "#<%= options[:hook_name] -%>" do
    setup do
    end

    should "be tested"
  end
end
